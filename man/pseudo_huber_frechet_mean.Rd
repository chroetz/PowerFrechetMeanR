% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudo_huber_frechet_mean.R
\name{pseudo_huber_frechet_mean}
\alias{pseudo_huber_frechet_mean}
\title{Compute the Pseudo-Huber Frechet Mean}
\usage{
pseudo_huber_frechet_mean(X, delta, tol = 1e-10, maxit = 1000L, ...)
}
\arguments{
\item{X}{Numeric matrix of data points, one row per observation
(\eqn{n \times d}).  A numeric vector is treated as an \eqn{n \times 1}
matrix.}

\item{delta}{Numeric vector of pseudo-Huber thresholds, each \eqn{> 0}.
Duplicate values are silently removed; the returned tibble rows are
sorted by \code{delta}.}

\item{tol}{Convergence tolerance.  Sets \code{control$factr} and
\code{control$pgtol} for L-BFGS-B.  Default \code{1e-10}.}

\item{maxit}{Maximum number of L-BFGS-B iterations (default \code{1000}).}

\item{...}{Additional entries for the \code{control} list of \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per unique value of \code{delta}
(sorted ascending), containing columns:
\describe{
\item{\code{delta}}{Pseudo-Huber threshold.}
\item{\code{mean}}{Matrix column (\eqn{k \times d}); row \eqn{i} is the
pseudo-Huber Frechet mean for \code{delta[i]}.  Access as
\code{res$mean[i, ]}.}
\item{\code{value}}{Minimised objective \eqn{f(x^*)}.}
\item{\code{convergence}}{Integer code from \code{\link[stats:optim]{stats::optim()}}; \code{0} = success.}
\item{\code{n}}{Number of data points.}
\item{\code{d}}{Dimension.}
}
}
\description{
Given \eqn{n} data points \eqn{x_1, \dots, x_n \in \mathbb{R}^d} and one
or more threshold parameters \eqn{\delta > 0}, the \strong{pseudo-Huber Frechet
mean} for each \eqn{\delta} is the minimiser of
\deqn{f(x) = \sum_{i=1}^{n} \rho_\delta(\|x_i - x\|),
  \quad x \in \mathbb{R}^d,}
where the pseudo-Huber loss is
\deqn{\rho_\delta(r) = \delta^2\!\left(\sqrt{1 + (r/\delta)^2} - 1\right).}
}
\details{
The pseudo-Huber loss is a smooth (infinitely differentiable) approximation
to the Huber loss.  For large \eqn{\delta}, \eqn{\rho_\delta(r) \approx
r^2/2} and the minimiser approaches the arithmetic mean.  For small
\eqn{\delta}, \eqn{\rho_\delta(r) \approx \delta r} and the minimiser
approaches the geometric median.
\subsection{Warm-start strategy}{

\eqn{\delta} values are sorted in \strong{decreasing} order and solved
sequentially; each solve is warm-started from the previous result.  The
largest \eqn{\delta} is anchored at \code{colMeans(X)}, which is the exact
minimiser in the limiting case \eqn{\delta \to \infty}.
}

\subsection{Optimisation}{

Uses an analytical gradient (C++/RcppArmadillo); the pseudo-Huber gradient
has no singularity and requires no smoothing constant.  Optimised with
L-BFGS-B from \code{\link[stats:optim]{stats::optim()}}.
}
}
\examples{
set.seed(1)
X <- matrix(rnorm(30), nrow = 10, ncol = 3)

# Single threshold: large delta -> arithmetic mean
res <- pseudo_huber_frechet_mean(X, delta = 1e6)
stopifnot(max(abs(res$mean[1, ] - colMeans(X))) < 1e-5)

# Vector of thresholds
res_multi <- pseudo_huber_frechet_mean(X, delta = c(0.5, 1, 2, 5))
res_multi

}
\seealso{
\code{\link[=pseudo_huber_objective_value]{pseudo_huber_objective_value()}} to evaluate the objective at an
arbitrary point; \code{\link[=huber_frechet_mean]{huber_frechet_mean()}} for the piecewise-linear Huber
variant.
}
